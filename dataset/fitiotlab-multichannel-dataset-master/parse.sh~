#!/bin/sh

sed 's/|/\n/g' results/1.txt | sed 's/,/ /g' | sort -V | awk ' NR>1 {
k = $1 OFS $2 OFS $3
}
{
arr[k] += $4;
count[k]++
}
END{
for (i in arr){
print i, arr[i]/count[i]
}
}
' | sort -V | head -n-1 > results/time_intervals/res1_time.txt
